v0.1.6 — OTA build context fix · Исправление build context при OTA

---

## English

### Fixed

- OTA deploy stopped during build: `unable to prepare context: path "/host_mnt/.../runtime" not found`
- Build contexts now use `PYORCH_BUILD_ROOT` (`/deploy` in updater); bind mounts keep host-resolved `PYORCH_HOST_PROJECT_ROOT`

**Full Changelog**: https://github.com/PyOrchestrator/PyOrchestrator/compare/v0.1.5...v0.1.6

---

## Русский

### Исправлено

- OTA останавливался на сборке: `unable to prepare context: path "/host_mnt/.../runtime" not found`
- Build contexts используют `PYORCH_BUILD_ROOT` (`/deploy` в updater); bind mounts — host-путь `PYORCH_HOST_PROJECT_ROOT`

**Полный changelog**: https://github.com/PyOrchestrator/PyOrchestrator/compare/v0.1.5...v0.1.6

