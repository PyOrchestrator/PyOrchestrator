v0.1.3 — OTA Compose project name fix · Исправление имени Compose-проекта при OTA

---

## English

### Fixed

- OTA update deploy failed with container name conflicts (`pyorch-postgres is already in use`)
- Updater now uses fixed Compose project name `pyorchestrator` via `name:` in compose file and `-p` flag in update scripts
- Stale `.git/index.lock` cleared before git operations during update/rollback

**Full Changelog**: https://github.com/PyOrchestrator/PyOrchestrator/compare/v0.1.2...v0.1.3

---

## Русский

### Исправлено

- OTA падал из-за конфликта имён контейнеров (`pyorch-postgres is already in use`)
- Updater использует фиксированное имя проекта Compose `pyorchestrator` (`name:` и флаг `-p`)
- Устаревший `.git/index.lock` очищается перед git-операциями при update/rollback

**Полный changelog**: https://github.com/PyOrchestrator/PyOrchestrator/compare/v0.1.2...v0.1.3

