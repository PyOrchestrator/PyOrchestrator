v0.1.7 — OTA APP_VERSION sync fix · Исправление синхронизации APP_VERSION при OTA

---

## English

### Fixed

- OTA completed in logs but UI still showed `0.1.0-dev` and update available — `.env` was not updated
- Version now read from deployed `config.py`; updater syncs `APP_VERSION` in `.env` before compose up

**Full Changelog**: https://github.com/PyOrchestrator/PyOrchestrator/compare/v0.1.6...v0.1.7

---

## Русский

### Исправлено

- OTA завершался в логах, но UI показывал `0.1.0-dev` — `.env` не обновлялся
- Версия читается из задеплоенного `config.py`; updater синхронизирует `APP_VERSION` в `.env` перед compose up

**Полный changelog**: https://github.com/PyOrchestrator/PyOrchestrator/compare/v0.1.6...v0.1.7

